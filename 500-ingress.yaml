apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minio
  annotations:
    route.openshift.io/termination: edge
spec:
  rules:
    - host: 'minio.apps.managed-dev.taco.moe'
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: minio
              port:
                number: 9000
    - host: 'minio-console.apps.managed-dev.taco.moe'
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: minio-console
              port:
                number: 9001
